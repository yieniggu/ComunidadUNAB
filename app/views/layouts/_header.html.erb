<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">ComunidadUNAB</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if logged_in? %>
      <ul class="navbar-nav mr-auto">
        <li class="nav-item nav-link">
          <%= link_to  "Mi Perfil", user_path(current_user) %>
        </li>
        <% if current_user.courses.any? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              Tus Cursos
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <% current_user.courses.each do |course| %>
                <%= link_to "#{course.name}", course_path(course), class: "course-link" %>
                <hr>
              <% end %>
          </li>
        <% end %>
      </ul>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <%= link_to  "Desconectarse", logout_path, method: :delete %>
        </li>
      </ul>
    <% end %>
  </div>
</nav>